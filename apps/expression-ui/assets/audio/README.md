# Audio assets

Optional local wake acknowledgement:

`wake_ack.wav`

The production asset-pack location is
`$RIVERBANK_HOME/.local/share/riverbank/assets/audio/wake_ack.wav`. The service
continues without this file. Supply a WAV you created or have the right to
redistribute, or point `RIVERBANK_WAKE_ACK_PATH` to another local file.
