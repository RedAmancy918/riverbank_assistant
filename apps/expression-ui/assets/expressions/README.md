# Expression assets

The production system uses a set of animated GIF expressions. Those files are
not included because their redistribution rights are not established.

Place your own assets here using the filenames referenced by
`../../expressions.json`, or change that JSON to match your files. Recommended
source size is 800×800 for the round DSI display; the renderer also accepts
other sizes and scales them to the configured output.
