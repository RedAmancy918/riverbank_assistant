[Unit]
# Keep the compact DSI handoff surface visible until the persistent renderer
# has created and announced its window.  The handoff service is Type=notify,
# so this ordering removes the black desktop gap without delaying animation
# preparation once the Wayland output is ready.
Wants=riverbank-boot-handoff.service
After=riverbank-boot-handoff.service
