[Service]
# The lightweight supervisor remains resident; the Hailo worker is process-
# isolated and loaded only while a TTL face-tracking lease is active. These
# weights only make it yield briefly when the latency-sensitive voice path is
# decoding or preparing its first spoken response.
CPUWeight=20
IOWeight=20
Nice=10
